package org.example;

public class PointNew extends Point{
    private boolean visited;

    public PointNew(int vid, double vx, double vy, boolean mVisited) {
        super(vid, vx, vy);
        visited = mVisited;


    }
}
